---------- LANGUAGE SQL ----------
INTEGER : int
VARCHAR : str
artist
    - artist_id
    - name

album
    - album_id
    - artiste_id
    - title
    - release_date


----- SQL Request -----

CREATE TABLE artist (
    artist_id INTEGER NOT NULL PRIMARY KEY,
    name VARCHAR
    );

CREATE TABLE album (
    album_id INTEGER NOT NULL PRIMARY KEY,
    artist_id INTEGER REFERENCES artist,
    title VARCHAR,
    release_date INTEGER,
    );

INSERT INTO artiste (name) VALUES ("Mickael Jackson")
INSERT INTO ALBUM (title, release_date) VALUES ("Thriller", 1983)

UPDATE album SET release_date = 1982 WHERE title;

DELETE FROM artist WHERE name = "Madonna";

SELECT * FROM artist;
SELECT name FROM artist;
SELECT * FROM album WHERE release_date > 1990;

SELECT name, title FROM album a JOIN artist ar ON a.artist_id = ar.artist_id;

SELECT title FROM album a JOIN artist ar ON a.artist_id = ar.artist_id AND ar.name = "Céline Dion";